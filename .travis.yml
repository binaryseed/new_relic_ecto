language: elixir
elixir: 1.9.4
otp_release:
  - 22.2
notifications:
  email:
    - vince@newrelic.com
services:
  - docker
script:
  - docker-compose up -d
  - mix format --check-formatted
  - mix test
  - cd examples/sample_app
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix test
